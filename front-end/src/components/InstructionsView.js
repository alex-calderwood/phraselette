import React from 'react';

const InstructionsView = () => (
  <div className="instructions-view">
    <p>Highlight text in the editor to begin exploring.</p>
    <p>Add palettes below. Each palette offers insights into textual dimensions such as word choice, sentence structure, and more.</p>
  </div>
);

export default InstructionsView;